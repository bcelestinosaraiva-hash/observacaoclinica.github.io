name = "observacaoclinica-comentarios"
main = "src/index.js"
compatibility_date = "2024-11-01"

# Variável pública (não sensível) — pode ficar aqui em texto puro.
[vars]
GOOGLE_CLIENT_ID = "107435859530-jacvqe4iubpt3qq23pto7mrs0sb2sngs.apps.googleusercontent.com"

# Banco D1 — crie primeiro com:
#   wrangler d1 create observacaoclinica-comentarios
# e cole o database_id retornado aqui embaixo.
[[d1_databases]]
binding = "DB"
database_name = "observacaoclinica-comentarios"
database_id = "9e31139d-8b05-4a9c-bc9d-efd5a76e83cc"

# Segredos (NÃO ficam neste arquivo — configure via terminal):
#   wrangler secret put ADMIN_TOKEN
#   wrangler secret put TURNSTILE_SECRET_KEY   (opcional, só se usar Turnstile)
